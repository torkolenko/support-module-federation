interface Endpoints {
  REQUESTS: string;
  STATUSES: string;
  TYPES: string;
}

export const endpoints: Endpoints = {
  REQUESTS: "/api/requests",
  STATUSES: "/api/statuses",
  TYPES: "/api/types",
};
